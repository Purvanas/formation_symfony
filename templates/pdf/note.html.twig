<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title|default('Note') }}</title>
    <style>
        body { font-family: DejaVu Sans, Arial, sans-serif; color: #222; }
        h1 { font-size: 20px; margin: 0 0 8px; }
        .meta { font-size: 12px; color: #666; margin-bottom: 16px; }
        .content { font-size: 14px; line-height: 1.5; }
        .footer { margin-top: 24px; font-size: 11px; color: #888; }
        table { border-collapse: collapse; width: 100%; margin-top: 12px; }
        th, td { border: 1px solid #ccc; padding: 8px; font-size: 12px; }
        th { background: #f7f7f7; text-align: left; }
    </style>
    </head>
<body>
    <h1>{{ title|default('Note') }}</h1>
    <div class="meta">
        Généré le {{ (date|default('now'))|date('d/m/Y H:i') }}
    </div>

    <div class="content">
        {% if items is defined and items is iterable and items|length > 0 %}
            <table>
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Désignation</th>
                        <th style="text-align:right">Montant (€)</th>
                    </tr>
                </thead>
                <tbody>
                    {% for item in items %}
                        <tr>
                            <td>{{ loop.index }}</td>
                            <td>{{ item.label }}</td>
                            <td style="text-align:right">{{ item.amount|number_format(2, ',', ' ') }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        {% else %}
            <p>Aucun contenu spécifique. Personnalisez ce template selon vos besoins.</p>
        {% endif %}
    </div>

    <div class="footer">
        Document généré automatiquement.
    </div>
</body>
</html>


