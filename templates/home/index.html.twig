{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
<div class="py-5">
    <div class="p-5 mb-4 bg-white rounded-3 shadow-sm">
        <div class="container-fluid py-5">
            <h1 class="display-5 fw-bold">Gestion de formations simplifiée 🎓</h1>
            <p class="col-md-8 fs-5 mt-3">
                Organisez vos projets, suivez vos clients et planifiez vos sessions en toute simplicité.
            </p>
            <div class="mt-4 d-flex gap-2">
                <a href="{{ path('projets_index') }}" class="btn btn-primary btn-lg">Voir les projets</a>
                <a href="{{ path('clients_index') }}" class="btn btn-outline-secondary btn-lg">Gérer les clients</a>
            </div>
        </div>
    </div>

    <div class="row g-4">
        <div class="col-12 col-md-4">
            <div class="card h-100">
                <div class="card-body">
                    <h5 class="card-title">📋 Projets</h5>
                    <p class="card-text">Créez, suivez et pilotez vos projets de formation.</p>
                    <a href="{{ path('projets_index') }}" class="stretched-link">Accéder</a>
                </div>
            </div>
        </div>
        <div class="col-12 col-md-4">
            <div class="card h-100">
                <div class="card-body">
                    <h5 class="card-title">🧑‍💼 Clients</h5>
                    <p class="card-text">Centralisez vos comptes clients et leurs demandes.</p>
                    <a href="{{ path('clients_index') }}" class="stretched-link">Accéder</a>
                </div>
            </div>
        </div>
        <div class="col-12 col-md-4">
            <div class="card h-100">
                <div class="card-body">
                    <h5 class="card-title">🔐 Accès</h5>
                    <p class="card-text">Connectez-vous pour accéder aux fonctionnalités avancées.</p>
                    {% if app.user %}
                        <span class="badge text-bg-success">Connecté</span>
                    {% else %}
                        <a href="{{ path('app_login') }}" class="stretched-link">Se connecter</a>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
